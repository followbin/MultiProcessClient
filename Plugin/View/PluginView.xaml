<UserControl x:Class="Plugin.PluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plugin"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <TextBlock FontSize="20" Name="ProcessID"></TextBlock>
            <TextBlock FontSize="20">新的tab界面</TextBlock>
            <TextBox Text="{Binding Content}"></TextBox>
            <Button Content="添加内容" Command="{Binding AddContent}"/>
            <Button Content="抛出异常" Command="{Binding ThrowException}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
